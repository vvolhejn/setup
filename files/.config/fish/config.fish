# Abbreviations expand a short command to a longer one.
# This is different from an alias because the expansion is done
# when the command is typed, not when it is run.
abbr --add prp poetry run python
abbr --add pr poetry run